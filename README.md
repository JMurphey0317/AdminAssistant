### 🤖 Admin Assistant

```markdown
# Admin AI Assistant (Prompt Builder) for Salesforce

Use natural language to generate Salesforce Flows, formulas, and Apex logic with the power of AI. Create rules with simple prompts like “Block leads without an email.”

## Features
- Natural language input
- Generates logic in real-time
- Boosts declarative build speed

## Installation
```bash
sfdx force:source:deploy -p force-app
